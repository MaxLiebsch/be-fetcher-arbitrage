{
  "name": "be-fetcher-arbitrage",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start-fetch": "cross-env NODE_ENV=development nodemon --ignore ./data/  --exec node ./index.js",
    "start": "cross-env NODE_ENV=production node ./index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cross-env": "^7.0.3",
    "fs-jetpack": "^5.1.0",
    "puppeteer": "^22.4.0",
    "underscore": "^1.13.6"
  }
}
